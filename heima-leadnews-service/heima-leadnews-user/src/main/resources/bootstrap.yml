server:
  port: 51801
spring:
  application:
    name: leadnews-user
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: hmtt
      config:
        server-addr: localhost:8848
        file-extension: yml
        namespace: hmtt
logging:
  level:
    sql: debug